<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">


    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal(category_id, name, price, description, image,status)
            VALUE(#{categoryId},#{name},#{price},#{description},#{image},#{status})
    </insert>
    <update id="updateSetmeal">
        UPDATE setmeal
        <set>
            <if test="categoryId != null">
                category_id = #{categoryId},
            </if>
            <if test="name != null and name != ''">
                name = #{name},
            </if>
            <if test="price != null">
                price = #{price},
            </if>
            <if test="description != null">
                description = #{description},
            </if>
            <if test="image != null and image != ''">
                image = #{image},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="updateTime != null">
                update_time = #{updateTime},
            </if>
            <if test="updateUser != null">
                update_user = #{updateUser}
            </if>
        </set>
        WHERE id = #{id}
    </update>
    <update id="startOrStop">
        UPDATE setmeal set status = #{status} where id =#{id}
    </update>
    <delete id="deleteByIds">
        delete from setmeal where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        and setmeal.status=0
    </delete>
    <select id="pageQuery" resultType="com.sky.vo.SetmealVO">
        select setmeal.*,category.name categoryName from setmeal left join category on setmeal.category_id = category.id
        <where>
            <if test="name !=null">
                and name like concat('%',#{name},'%'),
            </if>
            <if test="categoryId !=null">
                and category_id = #{categoryId},
            </if>
            <if test="status !=null">
                and status = #{status},
            </if>
        </where>
        order by create_time desc
    </select>
    <select id="getById" resultType="com.sky.entity.Setmeal">
        select * from setmeal where id =#{id}
    </select>
</mapper>
